<CheckBox>
    size_hint: None, None
    size: 26, 26
    # My own images doesn't work. They are way bigger than set size.
    # background_checkbox_down: app.img_path('chbx_checked.png')
    # background_checkbox_normal: app.img_path('chbx_unchecked.png')
    # background_radio_down: app.img_path('chbx_checked.png')
    # background_radio_normal: app.img_path('chbx_unchecked.png')

    canvas.before:
        Color:
            rgba: app.unactive_bcolor
        Rectangle:
            size: sp(20), sp(20)
            pos: int(self.center_x - sp(10)), int(self.center_y - sp(10))